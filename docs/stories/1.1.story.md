# Story 1.1 — Setup do Projeto Next.js + Supabase

**Epic:** EPIC-01 — Fundação & Infraestrutura  
**Status:** Draft  
**Agente:** @dev  
**Esforço estimado:** M (meio dia)  
**Dependências:** nenhuma — primeira story do projeto

---

## User Story

Como desenvolvedor,  
quero um projeto Next.js 14 configurado com TypeScript, Tailwind, shadcn/ui e Supabase conectado,  
para que o squad tenha uma base de desenvolvimento padronizada e funcional a partir da qual todas as outras stories possam ser construídas.

---

## Contexto Técnico

Esta é a story fundacional. Ela cria o esqueleto do projeto dentro da pasta `SmartPreço/` existente, que atualmente contém apenas documentação. O projeto Next.js será inicializado com `create-next-app` e configurado conforme a arquitetura aprovada em `docs/architecture/architecture.md`.

**Stack a instalar:**
- Next.js 14+ com App Router
- TypeScript 5+
- Tailwind CSS 3+
- shadcn/ui (tema base)
- Supabase JS client (`@supabase/ssr` + `@supabase/supabase-js`)
- Vitest (testes unitários)
- ESLint + Prettier

**Package manager:** pnpm (conforme ADR-04 da arquitetura)

---

## Acceptance Criteria

- [ ] **AC1:** Projeto criado com `pnpm create next-app` usando TypeScript, Tailwind e App Router
- [ ] **AC2:** shadcn/ui inicializado com `pnpm dlx shadcn@latest init` — tema "default", base color "slate"
- [ ] **AC3:** Componentes shadcn/ui instalados: `button`, `card`, `input`, `label`, `badge`, `tabs`, `dialog`, `select`, `table`, `tooltip`, `separator`
- [ ] **AC4:** Supabase configurado — dois clients criados:
  - `src/lib/supabase/client.ts` (browser)
  - `src/lib/supabase/server.ts` (server-side)
- [ ] **AC5:** Arquivo `.env.local` criado (não commitado) com variáveis:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
- [ ] **AC6:** `.env.example` atualizado com as variáveis acima (sem valores reais)
- [ ] **AC7:** Vitest configurado — `vitest.config.ts` presente, `pnpm test` roda sem erros
- [ ] **AC8:** ESLint + Prettier configurados — `pnpm lint` passa sem erros
- [ ] **AC9:** `pnpm dev` sobe localmente em `localhost:3000` sem erros no terminal
- [ ] **AC10:** `pnpm build` completa sem erros ou warnings de TypeScript

---

## Tarefas de Implementação

- [ ] 1. Inicializar projeto Next.js com pnpm dentro da pasta `SmartPreço/`
- [ ] 2. Instalar e configurar shadcn/ui com tema base
- [ ] 3. Instalar componentes shadcn/ui necessários
- [ ] 4. Instalar `@supabase/ssr` e `@supabase/supabase-js`
- [ ] 5. Criar `src/lib/supabase/client.ts`
- [ ] 6. Criar `src/lib/supabase/server.ts`
- [ ] 7. Criar `.env.local` com valores reais do Supabase
- [ ] 8. Atualizar `.env.example` com chaves sem valores
- [ ] 9. Instalar e configurar Vitest + `@vitest/coverage-v8`
- [ ] 10. Criar `vitest.config.ts`
- [ ] 11. Criar teste de smoke: `tests/unit/smoke.test.ts` (1+1=2)
- [ ] 12. Ajustar `package.json` scripts: `dev`, `build`, `test`, `lint`, `typecheck`
- [ ] 13. Configurar Prettier (`.prettierrc`)
- [ ] 14. Validar `pnpm dev`, `pnpm build`, `pnpm test`, `pnpm lint`

---

## Arquivos a Criar / Modificar

```
src/
  app/
    layout.tsx          (root layout com fonts e providers)
    page.tsx            (homepage temporária — redirect para /dashboard)
    globals.css         (imports Tailwind)
  lib/
    supabase/
      client.ts         (browser Supabase client)
      server.ts         (server Supabase client)
    utils/
      cn.ts             (helper classnames com clsx + tailwind-merge)
  components/
    ui/                 (gerado pelo shadcn — não editar manualmente)

tests/
  unit/
    smoke.test.ts       (teste de sanidade)

.env.example            (atualizar com novas variáveis)
.env.local              (criar — NÃO commitar)
vitest.config.ts        (novo)
.prettierrc             (novo)
package.json            (atualizar scripts)
next.config.ts          (configuração base)
tailwind.config.ts      (gerado pelo shadcn)
tsconfig.json           (gerado pelo create-next-app)
```

---

## Referências

- Arquitetura: `docs/architecture/architecture.md` — Seções 2, 3, 8
- PRD: `docs/prd/04-technical.md`
- Epic: `docs/epics/EPIC-01-EXECUTION.yaml`

---

## Definition of Done

- [ ] Todos os ACs marcados como concluídos
- [ ] `pnpm dev` → página em localhost:3000 sem erros
- [ ] `pnpm build` → build limpo
- [ ] `pnpm test` → testes passando
- [ ] `pnpm lint` → zero erros
- [ ] `pnpm typecheck` → zero erros
- [ ] `.env.local` NÃO commitado (verificar `.gitignore`)
- [ ] Story status atualizado para **Done**
