# Story 1.5 — Tipos de Domínio (Domain Types)

**Epic:** EPIC-01 — Fundação & Infraestrutura  
**Status:** Done  
**Agente:** @dev  
**Esforço estimado:** S  
**Dependências:** Story 1.1 concluída

---

## User Story

Como desenvolvedor do SmartPreço,  
quero tipos TypeScript centralizados para todas as entidades do domínio,  
para que motor de cálculo, API routes, componentes e banco compartilhem os mesmos contratos sem ambiguidade.

---

## Contexto Técnico

Esta story estabelece o **contrato de tipos** de todo o sistema. Nenhuma lógica de negócio aqui — apenas interfaces, types e enums que definem o que existe no SmartPreço.

Os tipos derivam de três fontes autorizadas:
1. `docs/architecture/schema.md` — estrutura das tabelas (Sku, SkuCalculation, MlFee, MlSearchCache)
2. `docs/architecture/architecture.md` § 7 — Motor de Cálculo (ViabilityInput, ViabilityResult)
3. `docs/epics/EPIC-02-EXECUTION.yaml` — campos esperados pelo formulário e painel de resultados

**Por que criar antes de qualquer lógica:**  
O motor de cálculo (Story 2.0), os API routes (EPIC-05) e os componentes React (EPIC-02/03/04) todos importam esses tipos. Criar os tipos primeiro garante que todo o squad compila contra o mesmo contrato — sem `any`, sem type casts, sem surpresas.

---

## Acceptance Criteria

- [x] **AC1:** `src/types/pricing.ts` com interfaces:
  - `ViabilityInput` — dados de entrada do formulário de custo
  - `CostBreakdown` — detalhamento de cada componente do custo
  - `ProfitabilityMetrics` — margem, ROI, break-even, preços calculados
  - `ViabilityResult` — resultado completo do cálculo (inclui CostBreakdown + ProfitabilityMetrics)
  - `ViabilityClassification` — `'viable' | 'attention' | 'not_viable'`
  - `ListingType` — `'free' | 'classic' | 'premium'`

- [x] **AC2:** `src/types/sku.ts` com interfaces:
  - `SkuStatus` — `'draft' | 'viable' | 'attention' | 'not_viable' | 'for_sale'`
  - `Sku` — espelho da tabela `skus` do banco
  - `SkuCalculation` — espelho da tabela `sku_calculations` do banco
  - `SkuWithLatestCalc` — Sku + último cálculo (para listagem)
  - `CreateSkuPayload` — dados para criar um SKU novo (sem id/timestamps)
  - `UpdateSkuPayload` — Partial dos campos editáveis

- [x] **AC3:** `src/types/mercado.ts` com interfaces:
  - `MlListing` — anúncio do Mercado Livre (campos usados pela app)
  - `MlSearchQuery` — parâmetros de busca
  - `MlRawResponse` — shape da resposta bruta da ML API
  - `CleanedSearchResult` — resultado após filtros de limpeza
  - `PositionBadge` — `'below' | 'aligned' | 'above'`
  - `MarketSummary` — estatísticas da base limpa (min, max, mediana, média, confiança)

- [x] **AC4:** `src/types/database.ts` com tipos derivados do Supabase:
  - Tipo `Database` conforme gerado pelo Supabase CLI (`supabase gen types`)
  - Helpers: `DbSku`, `DbSkuCalculation`, `DbMlFee`, `DbMlSearchCache` (extraídos do Database type)
  - Nota: se o Supabase ainda não estiver configurado, criar como placeholder comentado

- [x] **AC5:** `src/types/index.ts` re-exportando todos os tipos de forma organizada

- [x] **AC6:** Zero uso de `any` em todos os arquivos de tipos

- [x] **AC7:** `pnpm typecheck` limpo após criação dos tipos

---

## Tarefas de Implementação

- [x] 1. Criar `src/types/pricing.ts` — interfaces do motor de cálculo
- [x] 2. Criar `src/types/sku.ts` — interfaces da Central de SKUs
- [x] 3. Criar `src/types/mercado.ts` — interfaces da integração ML
- [x] 4. Criar `src/types/database.ts` — tipos Supabase (ou placeholder)
- [x] 5. Criar `src/types/index.ts` com re-exports organizados
- [x] 6. Rodar `pnpm typecheck` e corrigir erros

---

## Arquivos a Criar

```
src/
  types/
    pricing.ts        (novo — ViabilityInput, ViabilityResult, etc.)
    sku.ts            (novo — Sku, SkuCalculation, SkuStatus, etc.)
    mercado.ts        (novo — MlListing, MarketSummary, etc.)
    database.ts       (novo — Database type do Supabase)
    index.ts          (novo — re-exports)
```

---

## Snippets de Referência

### pricing.ts (estrutura esperada)
```typescript
export type ListingType = 'free' | 'classic' | 'premium'
export type ViabilityClassification = 'viable' | 'attention' | 'not_viable'

export interface ViabilityInput {
  productCost: number        // CMV — custo de aquisição do produto
  shippingCost: number       // custo de frete para o comprador
  packagingCost: number      // embalagem e materiais de envio
  taxRate: number            // imposto sobre venda (0-1, ex: 0.06 = 6%)
  overheadRate: number       // overhead geral (0-1, ex: 0.05 = 5%)
  targetMargin: number       // margem-alvo (0-1, ex: 0.20 = 20%)
  salePrice: number          // preço de venda a testar
  listingType: ListingType
  installments: number       // 1 a 12
}

export interface CostBreakdown {
  acquisition: number        // CMV
  commission: number         // taxa ML (listingType)
  installmentFee: number     // custo de parcelamento
  shipping: number           // frete
  packaging: number          // embalagem
  tax: number                // impostos
  overhead: number           // overhead
  total: number              // soma de todos
}

export interface ProfitabilityMetrics {
  grossRevenue: number
  totalCost: number
  profit: number
  marginPercent: number      // pode ser negativo
  roiPercent: number         // pode ser negativo
  breakEvenPrice: number
  minimumViablePrice: number // onde margem = 0%
  recommendedPrice: number   // onde margem = targetMargin
}

export interface ViabilityResult {
  input: ViabilityInput
  costBreakdown: CostBreakdown
  metrics: ProfitabilityMetrics
  classification: ViabilityClassification
  calculatedAt: string       // ISO 8601
}
```

### sku.ts (estrutura esperada)
```typescript
export type SkuStatus = 'draft' | 'viable' | 'attention' | 'not_viable' | 'for_sale'

export interface Sku {
  id: string
  userId: string
  name: string
  notes: string | null
  status: SkuStatus
  isForSale: boolean
  adoptedPrice: number | null
  createdAt: string
  updatedAt: string
}

export interface SkuCalculation {
  id: string
  skuId: string
  costData: ViabilityInput    // snapshot
  resultData: ViabilityResult // snapshot
  salePrice: number
  listingType: ListingType
  marginPercent: number | null
  roiPercent: number | null
  isViable: boolean | null
  isAdopted: boolean
  createdAt: string
}
```

### mercado.ts (estrutura esperada)
```typescript
export type PositionBadge = 'below' | 'aligned' | 'above'

export interface MlListing {
  id: string
  title: string
  price: number
  currencyId: string        // 'BRL'
  freeShipping: boolean
  isFulfillment: boolean    // Full
  sellerReputation: string | null
  soldQuantity: number
  thumbnail: string
  permalink: string
}

export interface MarketSummary {
  totalListings: number
  cleanListings: number
  confidencePercent: number
  minPrice: number
  maxPrice: number
  medianPrice: number
  avgPrice: number
  p25Price: number
  p75Price: number
}
```

---

## Referências

- Schema do banco: `docs/architecture/schema.md` § 9 (contratos JSONB)
- Motor de cálculo: `docs/architecture/architecture.md` § 7
- EPIC-02: `docs/epics/EPIC-02-EXECUTION.yaml`
- EPIC-05: `docs/epics/EPIC-05-EXECUTION.yaml`

---

## Definition of Done

- [ ] Todos os ACs marcados como concluídos
- [ ] `pnpm typecheck` — zero erros
- [ ] `pnpm lint` — zero erros
- [ ] Zero `any` nos arquivos de tipos
- [ ] Story status atualizado para **Done**
